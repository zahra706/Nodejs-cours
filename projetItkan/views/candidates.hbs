<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Liste des Candidats</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-r from-gray-100 to-gray-200 min-h-screen p-6 font-sans">

  <div class="max-w-6xl mx-auto">
    <h1 class="text-3xl font-extrabold text-center text-gray-800 mb-8">📋 Liste des Candidats</h1>

    <div class="flex justify-end mb-4">
      <a href="/" class="inline-flex items-center bg-indigo-600 text-white py-2 px-4 rounded-lg shadow-md hover:bg-indigo-700 transition duration-200">
        ➕ Nouvelle Inscription
      </a>
    </div>

    <div class="bg-white shadow-lg rounded-lg overflow-x-auto">
      <table class="min-w-full table-auto divide-y divide-gray-200 text-sm">
        <thead class="bg-indigo-50">
          <tr>
            <th class="px-6 py-3 text-left font-semibold text-gray-600 uppercase">Prénom</th>
            <th class="px-6 py-3 text-left font-semibold text-gray-600 uppercase">Nom</th>
            <th class="px-6 py-3 text-left font-semibold text-gray-600 uppercase">Email</th>
            <th class="px-6 py-3 text-left font-semibold text-gray-600 uppercase">Téléphone</th>
            <th class="px-6 py-3 text-left font-semibold text-gray-600 uppercase">Date</th>
            <th class="px-6 py-3 text-left font-semibold text-gray-600 uppercase">Motivation</th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-100">
          {{#each candidates}}
          <tr class="hover:bg-gray-50 transition">
            <td class="px-6 py-4 whitespace-nowrap text-gray-800">{{this.first_name}}</td>
            <td class="px-6 py-4 whitespace-nowrap text-gray-800">{{this.last_name}}</td>
            <td class="px-6 py-4 whitespace-nowrap text-blue-600">{{this.email}}</td>
            <td class="px-6 py-4 whitespace-nowrap text-gray-700">{{this.phone}}</td>
            <td class="px-6 py-4 whitespace-nowrap text-gray-600">{{this.created_at}}</td>
            <td class="px-6 py-4 text-gray-700">{{this.motivation}}</td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>

</body>
</html>
